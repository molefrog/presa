import styled from 'styled-components'

const ControlGroup = styled.div`
  display: inline-flex;
  background: white;
  box-shadow: 0 0 1px 0 rgba(0, 0, 0, 0.1), 0 2px 8px 0 rgba(0, 0, 0, 0.04);
  border-radius: 50px;
  padding: 4px;
`

export default ControlGroup
